import { container, main } from '../../../assets/styles/base';

const styles = {
    container,
    main,
    title: {
        fontSize: "4.2rem",
        fontWeight: "600",
        display: "inline-block",
        position: "relative"
    }
}

export default styles;
