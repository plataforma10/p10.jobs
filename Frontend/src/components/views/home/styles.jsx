import { container, main } from '../../../assets/styles/base';

const homeStyle = {
    container,
    main,
    title: {
        fontSize: "4.2rem",
        fontWeight: "600",
        display: "inline-block",
        position: "relative"
    }
}

export default homeStyle;
